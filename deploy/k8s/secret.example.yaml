# TEMPLATE — copy to secret.yaml (gitignored) and fill real values, or generate
# via `kubectl create secret`. Never commit real secrets.
apiVersion: v1
kind: Secret
metadata:
  name: reportly-secrets
  namespace: reportly
type: Opaque
stringData:
  DATABASE_URL: "postgres://reportly:CHANGE_ME@postgres:5432/reportly"
  LOG_DATABASE_URL: "postgres://reportly:CHANGE_ME@postgres:5432/reportly_logs"
  REDIS_URL: "redis://redis:6379"
