**/dist/**
**/build/**
**/coverage/**
**/.turbo/**
**/node_modules/**
pnpm-lock.yaml

# Generated by `pnpm docs:env` from the API's env schema. Prettier realigns the
# table columns, which would fight the generator on every run.
docs/reference/environment.md

# Migration snapshots written by drizzle-kit, for the same reason: it owns their
# formatting and rewrites them on every `db:generate`. Reformatting them here only
# guarantees a diff on the next generated migration.
apps/api/drizzle/meta/

# Playwright outputs and captured session state (also gitignored)
e2e/.auth/
e2e/test-results/
e2e/playwright-report/
